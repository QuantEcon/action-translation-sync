# Code Block Analysis Report

**Source**: `/Users/mmcky/work/quantecon/lecture-python-intro`  
**Target**: `/Users/mmcky/work/quantecon/lecture-intro.zh-cn`  
**Docs Folder**: `.`  
**Generated**: 2025-12-16T01:53:21.117Z  
**Tool Version**: 0.1.0

## Summary

| File | Source | Target | Matched | Modified | Missing | Extra | Score | Notes |
|------|--------|--------|---------|----------|---------|-------|-------|-------|
| `README.md` | 0 | 2 | 0 | 0 | 0 | 2 | ✅ 100% | - |
| `lectures/ar1_processes.md` | 15 | 12 | 10 | 2 | 3 | 0 | 🟠 67% | 📍 i18n |
| `lectures/business_cycle.md` | 24 | 24 | 7 | 17 | 0 | 0 | 🔴 29% | 📍 i18n |
| `lectures/cagan_adaptive.md` | 7 | 9 | 1 | 6 | 0 | 2 | 🔴 14% | - |
| `lectures/cagan_ree.md` | 9 | 9 | 5 | 4 | 0 | 0 | 🔴 56% | 📍 i18n |
| `lectures/cobweb.md` | 19 | 19 | 17 | 2 | 0 | 0 | 🟡 89% | 📍 i18n |
| `lectures/commod_price.md` | 6 | 6 | 2 | 4 | 0 | 0 | 🔴 33% | 📍 i18n |
| `lectures/complex_and_trig.md` | 10 | 10 | 6 | 4 | 0 | 0 | 🟠 60% | 📍 i18n |
| `lectures/cons_smooth.md` | 20 | 20 | 16 | 4 | 0 | 0 | 🟡 80% | 📍 i18n |
| `lectures/eigen_I.md` | 30 | 30 | 22 | 8 | 0 | 0 | 🟠 73% | 📍 i18n |
| `lectures/eigen_II.md` | 16 | 16 | 16 | 0 | 0 | 0 | ✅ 100% | - |
| `lectures/equalizing_difference.md` | 26 | 26 | 16 | 10 | 0 | 0 | 🟠 62% | 📍 i18n |
| `lectures/french_rev.md` | 21 | 21 | 18 | 3 | 0 | 0 | 🟡 86% | 📍 i18n |
| `lectures/geom_series.md` | 12 | 12 | 8 | 4 | 0 | 0 | 🟠 67% | 📍 i18n |
| `lectures/greek_square.md` | 13 | 13 | 12 | 1 | 0 | 0 | 🟨 92% | 📍 i18n |
| `lectures/heavy_tails.md` | 34 | 34 | 25 | 9 | 0 | 0 | 🟠 74% | 📍 i18n |
| `lectures/inequality.md` | 52 | 52 | 40 | 12 | 0 | 0 | 🟠 77% | 📍 i18n |
| `lectures/inflation_history.md` | 19 | 19 | 7 | 12 | 0 | 0 | 🔴 37% | 📍 i18n |
| `lectures/input_output.md` | 18 | 18 | 17 | 1 | 0 | 0 | 🟨 94% | 📍 i18n |
| `lectures/intro_supply_demand.md` | 22 | 22 | 21 | 1 | 0 | 0 | 🟨 95% | 📍 i18n |
| `lectures/laffer_adaptive.md` | 10 | 10 | 6 | 4 | 0 | 0 | 🟠 60% | 📍 i18n |
| `lectures/lake_model.md` | 8 | 8 | 7 | 1 | 0 | 0 | 🟡 88% | 📍 i18n |
| `lectures/linear_equations.md` | 34 | 34 | 33 | 1 | 0 | 0 | 🟨 97% | 📍 i18n |
| `lectures/lln_clt.md` | 13 | 13 | 9 | 4 | 0 | 0 | 🟠 69% | 📍 i18n |
| `lectures/long_run_growth.md` | 24 | 24 | 19 | 5 | 0 | 0 | 🟠 79% | 📍 i18n |
| `lectures/lp_intro.md` | 26 | 26 | 23 | 3 | 0 | 0 | 🟡 88% | 📍 i18n |
| `lectures/markov_chains_I.md` | 27 | 27 | 27 | 0 | 0 | 0 | ✅ 100% | - |
| `lectures/markov_chains_II.md` | 13 | 13 | 11 | 2 | 0 | 0 | 🟡 85% | 📍 i18n |
| `lectures/mle.md` | 24 | 24 | 18 | 6 | 0 | 0 | 🟠 75% | 📍 i18n |
| `lectures/money_inflation.md` | 16 | 16 | 12 | 4 | 0 | 0 | 🟠 75% | 📍 i18n |
| `lectures/money_inflation_nonlinear.md` | 9 | 9 | 6 | 3 | 0 | 0 | 🟠 67% | 📍 i18n |
| `lectures/monte_carlo.md` | 25 | 25 | 24 | 1 | 0 | 0 | 🟨 96% | 📍 i18n |
| `lectures/networks.md` | 42 | 42 | 38 | 4 | 0 | 0 | 🟨 90% | 📍 i18n |
| `lectures/olg.md` | 22 | 22 | 21 | 1 | 0 | 0 | 🟨 95% | 📍 i18n |
| `lectures/prob_dist.md` | 57 | 57 | 44 | 13 | 0 | 0 | 🟠 77% | 📍 i18n |
| `lectures/pv.md` | 8 | 8 | 6 | 2 | 0 | 0 | 🟠 75% | 📍 i18n |
| `lectures/scalar_dynam.md` | 19 | 19 | 19 | 0 | 0 | 0 | ✅ 100% | - |
| `lectures/schelling.md` | 8 | 8 | 5 | 3 | 0 | 0 | 🟠 63% | 📍 i18n |
| `lectures/short_path.md` | 9 | 9 | 8 | 1 | 0 | 0 | 🟡 89% | - |
| `lectures/simple_linear_regression.md` | 41 | 41 | 31 | 10 | 0 | 0 | 🟠 76% | 📍 i18n |
| `lectures/solow.md` | 26 | 26 | 26 | 0 | 0 | 0 | ✅ 100% | - |
| `lectures/status.md` | 2 | 2 | 2 | 0 | 0 | 0 | ✅ 100% | - |
| `lectures/supply_demand_heterogeneity.md` | 9 | 9 | 4 | 5 | 0 | 0 | 🔴 44% | - |
| `lectures/supply_demand_multiple_goods.md` | 22 | 22 | 15 | 7 | 0 | 0 | 🟠 68% | 📍 i18n |
| `lectures/time_series_with_matrices.md` | 29 | 29 | 29 | 0 | 0 | 0 | ✅ 100% | - |
| `lectures/unpleasant.md` | 10 | 10 | 8 | 2 | 0 | 0 | 🟡 80% | 📍 i18n |
|------|--------|--------|---------|----------|---------|-------|-------|-------|
| **TOTAL** | **906** | **907** | **717** | **186** | **3** | **4** | **79%** | - |

### Score Calculation

```
Score = (Matched Blocks / Source Blocks) × 100%

Match Types:
  - identical:        Exact match
  - normalized-match: Same after normalizing strings/comments
  - modified:         Code differs
  - missing:          Block in source, not in target
  - extra:            Block in target, not in source

Diffs show NORMALIZED code:
  - comments  → "# << COMMENT >>"
  - strings   → "<< STRING >>"
  - captions  → "caption: << CAPTION >>"
This filters out expected translation differences.
```

### Localization Notes (📍 i18n)

Files with detected localization patterns (e.g., CJK font configuration):

| File | Patterns Detected | Blocks with Additions |
|------|-------------------|----------------------|
| `lectures/ar1_processes.md` | Chinese/CJK font configuration | 2 |
| `lectures/business_cycle.md` | Chinese/CJK font configuration | 2 |
| `lectures/cagan_ree.md` | Chinese/CJK font configuration | 1 |
| `lectures/cobweb.md` | Chinese/CJK font configuration | 1 |
| `lectures/commod_price.md` | Chinese/CJK font configuration | 2 |
| `lectures/complex_and_trig.md` | Chinese/CJK font configuration | 1 |
| `lectures/cons_smooth.md` | Chinese/CJK font configuration | 2 |
| `lectures/eigen_I.md` | Chinese/CJK font configuration | 6 |
| `lectures/equalizing_difference.md` | Chinese/CJK font configuration | 5 |
| `lectures/french_rev.md` | Chinese/CJK font configuration | 1 |
| `lectures/geom_series.md` | Chinese/CJK font configuration | 2 |
| `lectures/greek_square.md` | Chinese/CJK font configuration | 1 |
| `lectures/heavy_tails.md` | Chinese/CJK font configuration | 5 |
| `lectures/inequality.md` | Chinese/CJK font configuration | 8 |
| `lectures/inflation_history.md` | Chinese/CJK font configuration | 11 |
| `lectures/input_output.md` | Chinese/CJK font configuration | 1 |
| `lectures/intro_supply_demand.md` | Chinese/CJK font configuration | 1 |
| `lectures/laffer_adaptive.md` | Chinese/CJK font configuration | 1 |
| `lectures/lake_model.md` | Chinese/CJK font configuration | 1 |
| `lectures/linear_equations.md` | Chinese/CJK font configuration | 1 |
| `lectures/lln_clt.md` | Chinese/CJK font configuration | 1 |
| `lectures/long_run_growth.md` | Chinese/CJK font configuration | 2 |
| `lectures/lp_intro.md` | Chinese/CJK font configuration | 2 |
| `lectures/markov_chains_II.md` | Chinese/CJK font configuration | 1 |
| `lectures/mle.md` | Chinese/CJK font configuration | 1 |
| `lectures/money_inflation.md` | Chinese/CJK font configuration | 1 |
| `lectures/money_inflation_nonlinear.md` | Chinese/CJK font configuration | 1 |
| `lectures/monte_carlo.md` | Chinese/CJK font configuration | 1 |
| `lectures/networks.md` | Chinese/CJK font configuration | 1 |
| `lectures/olg.md` | Chinese/CJK font configuration | 1 |
| `lectures/prob_dist.md` | Chinese/CJK font configuration | 3 |
| `lectures/pv.md` | CJK font family, Chinese/CJK font configuration | 2 |
| `lectures/schelling.md` | Chinese/CJK font configuration | 1 |
| `lectures/simple_linear_regression.md` | Chinese/CJK font configuration | 1 |
| `lectures/supply_demand_multiple_goods.md` | Chinese/CJK font configuration | 1 |
| `lectures/unpleasant.md` | Chinese/CJK font configuration | 1 |

---

## File Details

### lectures/ar1_processes.md

**Summary**: 15 source → 12 target | Score: 🟠 67%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 2 block(s) have localization additions

#### Block 1 (ipython) - ⚠️ Modified

> Line count changed: 3 → 8

```diff
  import matplotlib.pyplot as plt
  plt.rcParams["<< STRING >>"] = (11, 5) # << COMMENT >>
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 12 (ipython3) - ⚠️ Modified

> Language changed: ipython3 → python, Line count changed: 2 → 27

```diff
+ n = 2000 # << COMMENT >>
+ 
+ # << COMMENT >>
  μ_next = a * μ + b
  s_next = np.sqrt(a**2 * s**2 + c**2)
+ 
+ # << COMMENT >>
+ ψ = norm(μ, s)
+ ψ_next = norm(μ_next, s_next)
+ 
+ # << COMMENT >>
+ x_draws = ψ.rvs(n) # << COMMENT >>
+ x_draws_next = a * x_draws + b + c * np.random.randn(n) # << COMMENT >>
+ 
+ # << COMMENT >>
+ kde = KDE(x_draws_next)
+ 
+ # << COMMENT >>
+ x_grid = np.linspace(μ - 1, μ + 1, 100)
+ fig, ax = plt.subplots()
+ 
+ ax.plot(x_grid, ψ.pdf(x_grid), label="<< STRING >>")
+ ax.plot(x_grid, ψ_next.pdf(x_grid), label="<< STRING >>")
+ ax.plot(x_grid, kde.f(x_grid), label="<< STRING >>")
+ 
+ ax.legend()
+ plt.show()
```

#### Block 13 (ipython3) - ❌ Missing in Target

> Block missing in target

**Source block** (missing in target):
```ipython3
ψ = lambda x: K((x - μ) / s)
ψ_next = lambda x: K((x - μ_next) / s_next)
```

#### Block 14 (ipython3) - ❌ Missing in Target

> Block missing in target

**Source block** (missing in target):
```ipython3
ψ = norm(μ, s)
ψ_next = norm(μ_next, s_next)
```

#### Block 15 (ipython3) - ❌ Missing in Target

> Block missing in target

**Source block** (missing in target):
```ipython3
n = 2000
x_draws = ψ.rvs(n)
x_draws_next = a * x_draws + b + c * np.random.randn(n)
kde = KDE(x_draws_next)

x_grid = np.linspace(μ - 1, μ + 1, 100)
fig, ax = plt.subplots()

ax.plot(x_grid, ψ.pdf(x_grid), label="$\psi_t$")
ax.plot(x_grid, ψ_next.pdf(x_grid), label="$\psi_{t+1}$")
ax.plot(x_grid, kde.f(x_grid), label="estimate of $\psi_{t+1}$")

ax.legend()
plt.show()
```


### lectures/business_cycle.md

**Summary**: 24 source → 24 target | Score: 🔴 29%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 2 block(s) have localization additions

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 5 → 10

```diff
  import wbgapi as wb
  import pandas_datareader.data as web
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 3 (ipython3) - ⚠️ Modified

> Line count changed: 6 → 8

```diff
  cycler = plt.cycler(linestyle=['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>'],
  color=['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>'<< STRING >>'<< STRING >>', prop_cycle=cycler)
+ 
+ name_cn = pd.read_csv('<< STRING >>').set_index('<< STRING >>')
```

#### Block 9 (ipython3) - ⚠️ Modified

> Line count changed: 71 → 69

```diff
  "<< STRING >>""<< STRING >>""<< STRING >>"
  
- ax.plot(data.loc[country], label=country, **g_params)
+ country_cn = name_cn.loc[country]['<< STRING >>']
  
+ ax.plot(data.loc[country_cn], label=country, **g_params)
+ 
  # << COMMENT >>
  ax.axvspan(1973, 1975, **b_params)
...
  else:
  ylim = ax.get_ylim()[1]
- ax.text(1974, ylim + ylim*txt_pos,
- '<< STRING >>', **t_params)
- ax.text(1991, ylim + ylim*txt_pos,
- '<< STRING >>', **t_params)
- ax.text(2008, ylim + ylim*txt_pos,
- '<< STRING >>', **t_params)
- ax.text(2020, ylim + ylim*txt_pos,
- '<< STRING >>', **t_params)
+ ax.text(1974, ylim + ylim*txt_pos, '<< STRING >>', **t_params)
+ ax.text(1991, ylim + ylim*txt_pos, '<< STRING >>', **t_params)
+ ax.text(2008, ylim + ylim*txt_pos, '<< STRING >>', **t_params)
+ ax.text(2020, ylim + ylim*txt_pos, '<< STRING >>', **t_params)
  
  # << COMMENT >>
```

#### Block 10 (ipython3) - ⚠️ Modified

> Line count changed: 15 → 14

```diff
  name: us_gdp
  ---
- 
  fig, ax = plt.subplots()
  
```

#### Block 11 (ipython3) - ⚠️ Modified

> Line count changed: 14 → 13

```diff
  name: uk_gdp
  ---
- 
  fig, ax = plt.subplots()
  
```

#### Block 12 (ipython3) - ⚠️ Modified

> Line count changed: 14 → 13

```diff
  name: jp_gdp
  ---
- 
  fig, ax = plt.subplots()
  
```

#### Block 13 (ipython3) - ⚠️ Modified

> Line count changed: 14 → 13

```diff
  name: gc_gdp
  ---
- 
  fig, ax = plt.subplots()
  
```

#### Block 14 (ipython3) - ⚠️ Modified

> Line count changed: 14 → 13

```diff
  name: arg_gdp
  ---
- 
  fig, ax = plt.subplots()
  
```

#### Block 16 (ipython3) - ⚠️ Modified

> Line count changed: 48 → 46

```diff
  tags: [hide-input]
  ---
+ # << COMMENT >>
+ years = [datetime.datetime(year, 6, 1) for year in range(1942, 1948)]
+ unrate_census = [4.7, 1.9, 1.2, 1.9, 3.9, 3.9]
  
+ unrate_census = {'<< STRING >>': years, '<< STRING >>': unrate_census}
+ unrate_census = pd.DataFrame(unrate_census)
+ unrate_census.set_index('<< STRING >>', inplace=True)
+ 
  # << COMMENT >>
+ start_date = datetime.datetime(1929, 1, 1)
+ end_date = datetime.datetime(2022, 12, 31)
  
+ nber = web.DataReader('<< STRING >>', '<< STRING >>', start_date, end_date)
+ 
+ fig, ax = plt.subplots()
+ 
+ ax.plot(unrate_history, **g_params,
+ color='<< STRING >>',
+ linestyle='<< STRING >>', linewidth=2)
+ ax.plot(unrate_census, **g_params,
+ color='<< STRING >>', linestyle='<< STRING >>',
+ label='<< STRING >>', linewidth=2)
+ ax.plot(unrate, **g_params, color='<< STRING >>',
+ linestyle='<< STRING >>', linewidth=2)
+ 
+ # << COMMENT >>
+ ax.fill_between(nber.index, 0, 1,
+ where=nber['<< STRING >>']==1,
+ color='<< STRING >>', edgecolor='<< STRING >>',
+ alpha=0.3,
+ transform=ax.get_xaxis_transform(),
+ label='<< STRING >>')
+ ax.set_ylim([0, ax.get_ylim()[1]])
+ ax.legend(loc='<< STRING >>',
+ bbox_to_anchor=(0.5, 1.1),
+ ncol=3, fancybox=True, shadow=True)
+ ax.set_ylabel('<< STRING >>')
+ 
  plt.show()
```

#### Block 17 (ipython3) - ⚠️ Modified

> Line count changed: 74 → 68

```diff
- ---
- tags: [hide-input]
- ---
+ :tags: [hide-input]
  
  def plot_comparison(data, countries,
...
  # << COMMENT >>
  for country in countries:
- ax.plot(data.loc[country], label=country, **g_params)
+ country_cn = name_cn.loc[country]['<< STRING >>']
+ ax.plot(data.loc[country_cn], label=country, **g_params)
  
  # << COMMENT >>
...
  ax.set_ylim([-y_lim, y_lim])
  ylim = ax.get_ylim()[1]
- ax.text(1974, ylim + ylim*txt_pos,
- '<< STRING >>', **t_params)
- ax.text(1991, ylim + ylim*txt_pos,
- '<< STRING >>', **t_params)
- ax.text(2008, ylim + ylim*txt_pos,
- '<< STRING >>', **t_params)
- ax.text(2020, ylim + ylim*txt_pos,
- '<< STRING >>', **t_params)
+ ax.text(1974, ylim + ylim*txt_pos, '<< STRING >>', **t_params)
+ ax.text(1991, ylim + ylim*txt_pos, '<< STRING >>', **t_params)
+ ax.text(2008, ylim + ylim*txt_pos, '<< STRING >>', **t_params)
+ ax.text(2020, ylim + ylim*txt_pos, '<< STRING >>', **t_params)
  if baseline != None:
  ax.hlines(y=baseline, xmin=ax.get_xlim()[0],
```

#### Block 18 (ipython3) - ⚠️ Modified

> Line count changed: 11 → 8

```diff
- ---
- tags: [hide-input]
- ---
+ :tags: [hide-input]
  
  # << COMMENT >>
```

#### Block 19 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  figure:
  caption: << CAPTION >>
- name: adv_gdp
+ name: global_gdp_comparison
  tags: [hide-input]
  ---
- 
  fig, ax = plt.subplots()
+ 
  countries = ['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>']
  ylabel = '<< STRING >>'
- plot_comparison(gdp_growth.loc[countries, 1962:],
- countries, ylabel,
- 0.1, 20, ax,
+ plot_comparison(gdp_growth, countries,
+ ylabel, 0.1, 20, ax,
  g_params, b_params, t_params)
+ 
  plt.show()
```

#### Block 20 (ipython3) - ⚠️ Modified

> Line count changed: 15 → 14

```diff
  tags: [hide-input]
  ---
- 
  fig, ax = plt.subplots()
  countries = ['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>']
- plot_comparison(gdp_growth.loc[countries, 1962:],
+ plot_comparison(gdp_growth.loc[name_cn.loc[countries]['<< STRING >>'], 1962:],
  countries, ylabel,
  0.1, 20, ax,
```

#### Block 21 (ipython3) - ⚠️ Modified

> Line count changed: 21 → 20

```diff
  tags: [hide-input]
  ---
- 
  unempl_rate = wb.data.DataFrame('<< STRING >>',
  ['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>'], labels=True)
```

#### Block 22 (ipython3) - ⚠️ Modified

> Line count changed: 57 → 56

```diff
  tags: [hide-input]
  ---
- 
  start_date = datetime.datetime(1978, 1, 1)
  end_date = datetime.datetime(2022, 12, 31)
```

#### Block 23 (ipython3) - ⚠️ Modified

> Line count changed: 29 → 28

```diff
  tags: [hide-input]
  ---
- 
  start_date = datetime.datetime(1919, 1, 1)
  end_date = datetime.datetime(2022, 12, 31)
```

#### Block 24 (ipython3) - ⚠️ Modified

> Line count changed: 21 → 20

```diff
  tags: [hide-input]
  ---
- 
  private_credit = wb.data.DataFrame('<< STRING >>',
  ['<< STRING >>'], labels=True)
```


### lectures/cagan_adaptive.md

**Summary**: 7 source → 9 target | Score: 🔴 14%

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 7 → 5

```diff
  ["<< STRING >>", "<< STRING >>", "<< STRING >>", "<< STRING >>", "<< STRING >>"])
  
- def create_cagan_adaptive_model(α = 5, m0 = 1, Eπ0 = 0.5, T=80, λ = 0.9):
+ def create_cagan_model(α, m0, Eπ0, T, λ):
  return Cagan_Adaptive(α, m0, Eπ0, T, λ)
- 
- md = create_cagan_adaptive_model()
```

#### Block 3 (ipython3) - ⚠️ Modified

> Line count changed: 26 → 11

```diff
- def solve_cagan_adaptive(model, μ_seq):
- "<< STRING >>"
- α, m0, Eπ0, T, λ = model
- 
- A = np.eye(T+2, T+2) - λ*np.eye(T+2, T+2, k=-1)
- B = np.eye(T+2, T+1, k=-1)
- C = -α*np.eye(T+1, T+2) + α*np.eye(T+1, T+2, k=1)
- Eπ0_seq = np.append(Eπ0, np.zeros(T+1))
- 
  # << COMMENT >>
- Eπ_seq = np.linalg.solve(A - (1-λ)*B @ C, (1-λ) * B @ μ_seq + Eπ0_seq)
+ T = 80
+ T1 = 60
+ α = 5
+ λ = 0.9
+ m0 = 1
  
- # << COMMENT >>
- π_seq = μ_seq + C @ Eπ_seq
+ μ0 = 0.5
+ μ_star = 0
  
- D = np.eye(T+1, T+1) - np.eye(T+1, T+1, k=-1) # << COMMENT >>
- m0_seq = np.append(m0, np.zeros(T))
- 
- # << COMMENT >>
- m_seq = np.linalg.solve(D, μ_seq + m0_seq)
- m_seq = np.append(m0, m_seq)
- 
- # << COMMENT >>
- p_seq = m_seq + α * Eπ_seq
- 
- return π_seq, Eπ_seq, m_seq, p_seq
+ md = create_cagan_model(α=α, m0=m0, Eπ0=μ0, T=T, λ=λ)
```

#### Block 4 (ipython3) - ⚠️ Modified

> Line count changed: 27 → 28

```diff
- def solve_and_plot(model, μ_seq):
+ def solve(model, μ_seq):
+ "<< STRING >>"
  
- π_seq, Eπ_seq, m_seq, p_seq = solve_cagan_adaptive(model, μ_seq)
+ model_params = model.α, model.m0, model.Eπ0, model.T, model.λ
+ α, m0, Eπ0, T, λ = model_params
  
- T_seq = range(model.T+2)
+ A = np.eye(T+2, T+2) - λ*np.eye(T+2, T+2, k=-1)
+ B = np.eye(T+2, T+1, k=-1)
+ C = -α*np.eye(T+1, T+2) + α*np.eye(T+1, T+2, k=1)
+ Eπ0_seq = np.append(Eπ0, np.zeros(T+1))
  
- fig, ax = plt.subplots(5, 1, figsize=[5, 12], dpi=200)
- ax[0].plot(T_seq[:-1], μ_seq)
- ax[1].plot(T_seq[:-1], π_seq, label=r'<< STRING >>')
- ax[1].plot(T_seq, Eπ_seq, label=r'<< STRING >>')
- ax[2].plot(T_seq, m_seq - p_seq)
- ax[3].plot(T_seq, m_seq)
- ax[4].plot(T_seq, p_seq)
+ # << COMMENT >>
+ Eπ_seq = np.linalg.inv(A - (1-λ)*B @ C) @ ((1-λ) * B @ μ_seq + Eπ0_seq)
  
- y_labs = [r'<< STRING >>', r'<< STRING >>', r'<< STRING >>', r'<< STRING >>', r'<< STRING >>']
- subplot_title = [r'<< STRING >>', r'<< STRING >>', r'<< STRING >>', r'<< STRING >>', r'<< STRING >>']
+ # << COMMENT >>
+ π_seq = μ_seq + C @ Eπ_seq
  
- for i in range(5):
- ax[i].set_xlabel(r'<< STRING >>')
- ax[i].set_ylabel(y_labs[i])
- ax[i].set_title(subplot_title[i])
+ D = np.eye(T+1, T+1) - np.eye(T+1, T+1, k=-1)
+ m0_seq = np.append(m0, np.zeros(T))
  
- ax[1].legend()
- plt.tight_layout()
- plt.show()
+ # << COMMENT >>
+ m_seq = np.linalg.inv(D) @ (μ_seq + m0_seq)
+ m_seq = np.append(m0, m_seq)
  
+ # << COMMENT >>
+ p_seq = m_seq + α * Eπ_seq
+ 
  return π_seq, Eπ_seq, m_seq, p_seq
```

#### Block 5 (ipython3) - ⚠️ Modified

> Line count changed: 1 → 25, Print statement changes detected

```diff
- print(np.abs((md.λ - md.α*(1-md.λ))/(1 - md.α*(1-md.λ))))
+ def solve_and_plot(model, μ_seq):
+ 
+ π_seq, Eπ_seq, m_seq, p_seq = solve(model, μ_seq)
+ 
+ T_seq = range(model.T+2)
+ 
+ fig, ax = plt.subplots(5, 1, figsize=[5, 12], dpi=200)
+ ax[0].plot(T_seq[:-1], μ_seq)
+ ax[1].plot(T_seq[:-1], π_seq, label=r'<< STRING >>')
+ ax[1].plot(T_seq, Eπ_seq, label=r'<< STRING >>')
+ ax[2].plot(T_seq, m_seq - p_seq)
+ ax[3].plot(T_seq, m_seq)
+ ax[4].plot(T_seq, p_seq)
+ 
+ y_labs = [r'<< STRING >>', r'<< STRING >>', r'<< STRING >>', r'<< STRING >>', r'<< STRING >>']
+ 
+ for i in range(5):
+ ax[i].set_xlabel(r'<< STRING >>')
+ ax[i].set_ylabel(y_labs[i])
+ 
+ ax[1].legend()
+ plt.tight_layout()
+ plt.show()
+ 
+ return π_seq, Eπ_seq, m_seq, p_seq
```

#### Block 6 (ipython3) - ⚠️ Modified

> Line count changed: 9 → 1, Print statement changes detected

```diff
- # << COMMENT >>
- T1 = 60
- μ0 = 0.5
- μ_star = 0
- 
- μ_seq_1 = np.append(μ0*np.ones(T1), μ_star*np.ones(md.T+1-T1))
- 
- # << COMMENT >>
- π_seq_1, Eπ_seq_1, m_seq_1, p_seq_1 = solve_and_plot(md, μ_seq_1)
+ print(np.abs((λ - α*(1-λ))/(1 - α*(1-λ))))
```

#### Block 7 (ipython3) - ⚠️ Modified

> Line count changed: 8 → 1, Print statement changes detected

```diff
- # << COMMENT >>
- ϕ = 0.9
- μ_seq_2 = np.array([ϕ**t * μ0 + (1-ϕ**t)*μ_star for t in range(md.T)])
- μ_seq_2 = np.append(μ_seq_2, μ_star)
- 
- # << COMMENT >>
- π_seq_2, Eπ_seq_2, m_seq_2, p_seq_2 = solve_and_plot(md, μ_seq_2)
+ print(λ - α*(1-λ))
```

#### Block 8 (ipython3) - ➕ Extra in Target

> Extra block in target

**Target block** (not in source):
```ipython3
μ_seq_1 = np.append(μ0*np.ones(T1), μ_star*np.ones(T+1-T1))

# 求解并绘图
π_seq_1, Eπ_seq_1, m_seq_1, p_seq_1 = solve_and_plot(md, μ_seq_1)
```

#### Block 9 (ipython3) - ➕ Extra in Target

> Extra block in target

**Target block** (not in source):
```ipython3
# 参数
ϕ = 0.9
μ_seq_2 = np.array([ϕ**t * μ0 + (1-ϕ**t)*μ_star for t in range(T)])
μ_seq_2 = np.append(μ_seq_2, μ_star)


# 求解并绘图
π_seq_2, Eπ_seq_2, m_seq_2, p_seq_2 = solve_and_plot(md, μ_seq_2)
```


### lectures/cagan_ree.md

**Summary**: 9 source → 9 target | Score: 🔴 56%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 9

```diff
  from collections import namedtuple
  import matplotlib.pyplot as plt
+ plt.rcParams['<< STRING >>'] = 200
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 5 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  def plot_sequences(sequences, labels):
- fig, axs = plt.subplots(len(sequences), 1, figsize=(5, 12), dpi=200)
+ fig, axs = plt.subplots(len(sequences), 1, figsize=(5, 12))
  for ax, seq, label in zip(axs, sequences, labels):
  ax.plot(range(len(seq)), seq, label=label)
```

#### Block 7 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  # << COMMENT >>
- fig, ax = plt.subplots(5, 1, figsize=(5, 12), dpi=200)
+ fig, ax = plt.subplots(5, 1, figsize=(5, 12))
  
  # << COMMENT >>
```

#### Block 8 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  # << COMMENT >>
- fig, ax = plt.subplots(5, figsize=(5, 12), dpi=200)
+ fig, ax = plt.subplots(5, figsize=(5, 12))
  
  plot_configs = [
```


### lectures/cobweb.md

**Summary**: 19 source → 19 target | Score: 🟡 89%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 2 → 7

```diff
  import numpy as np
  import matplotlib.pyplot as plt
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 15 (ipython3) - ⚠️ Modified

> Line count changed: 27 → 26

```diff
  ax.legend(loc='<< STRING >>', fontsize=10)
  ax.set_xticks(np.arange(ts_length))
- ax.set_xlabel("<< STRING >>")
- ax.set_ylabel("<< STRING >>")
+ ax.set_xlabel(r"<< STRING >>")
+ ax.set_ylabel(r"<< STRING >>")
  plt.show()
```


### lectures/commod_price.md

**Summary**: 6 source → 6 target | Score: 🔴 33%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 2 block(s) have localization additions

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 6 → 11

```diff
  from scipy.optimize import brentq
  from scipy.stats import beta
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 3 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  :tags: [hide-input, hide-output]
  
- s = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>')['<< STRING >>']
+ s = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>', auto_adjust=False)['<< STRING >>']
```

#### Block 4 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  ax.plot(s, marker='<< STRING >>', alpha=0.5, ms=1)
- ax.set_ylabel('<< STRING >>', fontsize=12)
- ax.set_xlabel('<< STRING >>', fontsize=12)
+ ax.set_ylabel(r'<< STRING >>', fontsize=12)
+ ax.set_xlabel(r'<< STRING >>', fontsize=12)
  
  plt.show()
```

#### Block 5 (ipython3) - ⚠️ Modified

> Line count changed: 48 → 49

```diff
  
  # << COMMENT >>
+ 
  p = interp1d(grid,
  p_array,
```


### lectures/complex_and_trig.md

**Summary**: 10 source → 10 target | Score: 🟠 60%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython) - ⚠️ Modified

> Line count changed: 5 → 10

```diff
  from sympy import (Symbol, symbols, Eq, nsolve, sqrt, cos, sin, simplify,
  init_printing, integrate)
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 7 (python3) - ⚠️ Modified

> Line count changed: 3 → 2

```diff
- print('The numerical solution for the integral of cos(ω)sin(ω) \
- from -π to π is:')
+ print('<< STRING >>')
  integrate(cos(ω) * sin(ω), (ω, -π, π))
```

#### Block 9 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 3

```diff
- print('The analytical solution for the integral of cos(ω)**2 \
- from -π to π is:')
+ print('<< STRING >>')
  
  integrate(cos(ω)**2, (ω, -pi, pi))
```

#### Block 10 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 3

```diff
- print('The analytical solution for the integral of sin(ω)**2 \
- from -π to π is:')
+ print('<< STRING >>')
  
  integrate(sin(ω)**2, (ω, -pi, pi))
```


### lectures/cons_smooth.md

**Summary**: 20 source → 20 target | Score: 🟡 80%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 2 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 8

```diff
  import matplotlib.pyplot as plt
  from collections import namedtuple
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 3 (ipython3) - ⚠️ Modified

> Line count changed: 19 → 18

```diff
  c_seq = c0*np.ones(T+1)
  
- # << COMMENT >>
  A = np.diag(-R*np.ones(T), k=-1) + np.eye(T+1)
  b = y_seq - c_seq
```

#### Block 5 (ipython3) - ⚠️ Modified

> Line count changed: 17 → 12

```diff
  T = cs_model.T
  
- fig, axes = plt.subplots(1, 2, figsize=(12,5))
+ plt.plot(range(T+1), y_seq, label='<< STRING >>')
+ plt.plot(range(T+1), c_seq, label='<< STRING >>')
+ plt.plot(range(T+2), a_seq, label='<< STRING >>')
+ plt.plot(range(T+2), np.zeros(T+2), '<< STRING >>')
  
- axes[0].plot(range(T+1), y_seq, label='<< STRING >>', lw=2)
- axes[0].plot(range(T+1), c_seq, label='<< STRING >>', lw=2)
- axes[1].plot(range(T+2), a_seq, label='<< STRING >>', color='<< STRING >>', lw=2)
- axes[0].set_ylabel(r'<< STRING >>')
- axes[1].set_ylabel(r'<< STRING >>')
- 
- for ax in axes:
- ax.plot(range(T+2), np.zeros(T+2), '<< STRING >>', lw=1, color='<< STRING >>')
- ax.legend()
- ax.set_xlabel(r'<< STRING >>')
- 
+ plt.legend()
+ plt.xlabel(r'<< STRING >>')
+ plt.ylabel(r'<< STRING >>')
  plt.show()
```

#### Block 7 (ipython3) - ⚠️ Modified

> Line count changed: 25 → 28

```diff
  T = cs_model.T
  
+ # << COMMENT >>
+ T = cs_model.T
+ 
  fig, axes = plt.subplots(1, 2, figsize=(12,5))
  
```


### lectures/eigen_I.md

**Summary**: 30 source → 30 target | Score: 🟠 73%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 6 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 6 → 11

```diff
  from matplotlib.patches import FancyArrowPatch
  from mpl_toolkits.mplot3d import proj3d
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 4 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  # << COMMENT >>
  # << COMMENT >>
- ax[0].set_title("<< STRING >>")
+ ax[0].set_title(r"<< STRING >>")
  
  # << COMMENT >>
...
  # << COMMENT >>
  # << COMMENT >>
- ax[1].set_title("<< STRING >>")
+ ax[1].set_title(r"<< STRING >>")
  
  plt.show()
```

#### Block 12 (ipython3) - ⚠️ Modified

> Code content differs

```diff
- grid_composition_transform(B,A) # << COMMENT >>
+ grid_composition_transform(B, A) # << COMMENT >>
```

#### Block 25 (ipython3) - ⚠️ Modified

> Line count changed: 39 → 44

```diff
+ ---
+ mystnb:
+ figure:
+ caption: << CAPTION >>
+ name: pow-dist
+ ---
  # << COMMENT >>
  A = np.array([[1, 0, 3],
...
  plt.xlabel('<< STRING >>')
  plt.ylabel('<< STRING >>')
- plt.title('<< STRING >>')
  _ = plt.plot(errors)
```

#### Block 26 (ipython3) - ⚠️ Modified

> Line count changed: 31 → 34

```diff
+ ---
+ mystnb:
+ figure:
+ caption: << CAPTION >>
+ name: pow-trajectory
+ ---
  # << COMMENT >>
  fig = plt.figure()
...
  ax.set_box_aspect(aspect=None, zoom=0.8)
  
- ax.set_title('<< STRING >>')
- 
  plt.show()
```

#### Block 28 (ipython3) - ⚠️ Modified

> Line count changed: 36 → 40

```diff
+ ---
+ mystnb:
+ figure:
+ caption: << CAPTION >>
+ name: eigen-conv
+ ---
  # << COMMENT >>
  x, y = np.meshgrid(np.linspace(-5, 5, 15),
...
  plt.xlabel("<< STRING >>")
  plt.ylabel("<< STRING >>")
- plt.title("<< STRING >>")
  plt.grid()
  plt.gca().set_aspect('<< STRING >>', adjustable='<< STRING >>')
```

#### Block 29 (ipython3) - ⚠️ Modified

> Line count changed: 61 → 65

```diff
- fig, ax = plt.subplots(1, 3, figsize=(15, 5))
+ ---
+ mystnb:
+ figure:
+ caption: << CAPTION >>
+ name: vector-field
+ ---
+ figure, ax = plt.subplots(1, 3, figsize=(15, 5))
  A = np.array([[sqrt(3) + 1, -2],
  [1, sqrt(3) - 1]])
...
  ax[i].set_aspect('<< STRING >>', adjustable='<< STRING >>')
  
- fig.suptitle("<< STRING >>")
  plt.show()
```

#### Block 30 (ipython3) - ⚠️ Modified

> Line count changed: 56 → 60

```diff
+ ---
+ mystnb:
+ figure:
+ caption: << CAPTION >>
+ name: 3d-vector-field
+ ---
  class Arrow3D(FancyArrowPatch):
  def __init__(self, xs, ys, zs, *args, **kwargs):
...
  ax.set_box_aspect(aspect=None, zoom=0.8)
  
- plt.title("<< STRING >>")
  plt.draw()
  plt.show()
```


### lectures/equalizing_difference.md

**Summary**: 26 source → 26 target | Score: 🟠 62%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 5 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 6

```diff
  import numpy as np
  import matplotlib.pyplot as plt
- from collections import namedtuple
- from sympy import Symbol, Lambda, symbols
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 21 → 23

```diff
- # << COMMENT >>
- EqDiffModel = namedtuple('<< STRING >>', '<< STRING >>')
+ class equalizing_diff:
+ "<< STRING >>""<< STRING >>""<< STRING >>"
  
- def create_edm(R=1.05, # << COMMENT >>
- T=40, # << COMMENT >>
- γ_h=1.01, # << COMMENT >>
- γ_c=1.01, # << COMMENT >>
- w_h0=1, # << COMMENT >>
- D=10, # << COMMENT >>
- ):
+ def __init__(self, R, T, γ_h, γ_c, w_h0, D=0, π=None):
+ # << COMMENT >>
+ self.R, self.γ_h, self.γ_c, self.w_h0, self.D = R, γ_h, γ_c, w_h0, D
+ self.T, self.π = T, π
  
- return EqDiffModel(R, T, γ_h, γ_c, w_h0, D)
+ def compute_gap(self):
+ R, γ_h, γ_c, w_h0, D = self.R, self.γ_h, self.γ_c, self.w_h0, self.D
+ T, π = self.T, self.π
  
- def compute_gap(model):
- R, T, γ_h, γ_c, w_h0, D = model
- 
  A_h = (1 - (γ_h/R)**(T+1)) / (1 - γ_h/R)
  A_c = (1 - (γ_c/R)**(T-3)) / (1 - γ_c/R) * (γ_c/R)**4
- ϕ = A_h / A_c + D / (w_h0 * A_c)
  
+ # << COMMENT >>
+ if π!=None:
+ A_c = π*A_c
+ 
+ ϕ = A_h/A_c + D/(w_h0*A_c)
  return ϕ
```

#### Block 3 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 27

```diff
- ex1 = create_edm()
- gap1 = compute_gap(ex1)
+ # << COMMENT >>
+ def ϕ_R(mc, R_new):
+ mc_new = equalizing_diff(R_new, mc.T, mc.γ_h, mc.γ_c, mc.w_h0, mc.D, mc.π)
+ return mc_new.compute_gap()
  
- gap1
+ ϕ_R = np.vectorize(ϕ_R)
+ 
+ # << COMMENT >>
+ def ϕ_γh(mc, γh_new):
+ mc_new = equalizing_diff(mc.R, mc.T, γh_new, mc.γ_c, mc.w_h0, mc.D, mc.π)
+ return mc_new.compute_gap()
+ 
+ ϕ_γh = np.vectorize(ϕ_γh)
+ 
+ # << COMMENT >>
+ def ϕ_γc(mc, γc_new):
+ mc_new = equalizing_diff(mc.R, mc.T, mc.γ_h, γc_new, mc.w_h0, mc.D, mc.π)
+ return mc_new.compute_gap()
+ 
+ ϕ_γc = np.vectorize(ϕ_γc)
+ 
+ # << COMMENT >>
+ def ϕ_π(mc, π_new):
+ mc_new = equalizing_diff(mc.R, mc.T, mc.γ_h, mc.γ_c, mc.w_h0, mc.D, π_new)
+ return mc_new.compute_gap()
+ 
+ ϕ_π = np.vectorize(ϕ_π)
```

#### Block 4 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 12, Print statement changes detected

```diff
  # << COMMENT >>
- ex2 = create_edm(D=0)
- gap2 = compute_gap(ex2)
- gap2
+ R = 1.05
+ T = 40
+ γ_h, γ_c = 1.01, 1.01
+ w_h0 = 1
+ D = 10
+ 
+ # << COMMENT >>
+ ex1 = equalizing_diff(R=R, T=T, γ_h=γ_h, γ_c=γ_c, w_h0=w_h0, D=D)
+ gap1 = ex1.compute_gap()
+ 
+ print(gap1)
```

#### Block 5 (ipython3) - ⚠️ Modified

> Line count changed: 8 → 4, Print statement changes detected

```diff
- R_arr = np.linspace(1, 1.2, 50)
- models = [create_edm(R=r) for r in R_arr]
- gaps = [compute_gap(model) for model in models]
- 
- plt.plot(R_arr, gaps)
- plt.xlabel(r'<< STRING >>')
- plt.ylabel(r'<< STRING >>')
- plt.show()
+ # << COMMENT >>
+ ex2 = equalizing_diff(R, T, γ_h, γ_c, w_h0, D=0)
+ gap2 = ex2.compute_gap()
+ print(gap2)
```

#### Block 6 (ipython3) - ⚠️ Modified

> Line count changed: 8 → 5

```diff
- γc_arr = np.linspace(1, 1.2, 50)
- models = [create_edm(γ_c=γ_c) for γ_c in γc_arr]
- gaps = [compute_gap(model) for model in models]
- 
- plt.plot(γc_arr, gaps)
+ R_arr = np.linspace(1, 1.2, 50)
+ plt.plot(R_arr, φ_R(ex1, R_arr))
  plt.xlabel(r'<< STRING >>')
  plt.ylabel(r'<< STRING >>')
```

#### Block 7 (ipython3) - ⚠️ Modified

> Line count changed: 8 → 5

```diff
  γh_arr = np.linspace(1, 1.1, 50)
- models = [create_edm(γ_h=γ_h) for γ_h in γh_arr]
- gaps = [compute_gap(model) for model in models]
- 
- plt.plot(γh_arr, gaps)
+ plt.plot(γh_arr, φ_γh(ex1, γh_arr))
  plt.xlabel(r'<< STRING >>')
  plt.ylabel(r'<< STRING >>')
```

#### Block 8 (ipython3) - ⚠️ Modified

> Line count changed: 26 → 5, Print statement changes detected

```diff
  # << COMMENT >>
- EqDiffModel = namedtuple('<< STRING >>', '<< STRING >>')
+ ex3 = equalizing_diff(R, T, γ_h, γ_c, w_h0, π=0.2)
+ gap3 = ex3.compute_gap()
  
- def create_edm_π(R=1.05, # << COMMENT >>
- T=40, # << COMMENT >>
- γ_h=1.01, # << COMMENT >>
- γ_c=1.01, # << COMMENT >>
- w_h0=1, # << COMMENT >>
- D=10, # << COMMENT >>
- π=0 # << COMMENT >>
- ):
- 
- return EqDiffModel(R, T, γ_h, γ_c, w_h0, D, π)
- 
- def compute_gap(model):
- R, T, γ_h, γ_c, w_h0, D, π = model
- 
- A_h = (1 - (γ_h/R)**(T+1)) / (1 - γ_h/R)
- A_c = (1 - (γ_c/R)**(T-3)) / (1 - γ_c/R) * (γ_c/R)**4
- 
- # << COMMENT >>
- A_c = π * A_c
- 
- ϕ = A_h / A_c + D / (w_h0 * A_c)
- return ϕ
+ print(gap3)
```

#### Block 9 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 5

```diff
- ex3 = create_edm_π(π=0.2)
- gap3 = compute_gap(ex3)
- 
- gap3
+ π_arr = np.linspace(0.2, 1, 50)
+ plt.plot(π_arr, φ_π(ex3, π_arr))
+ plt.ylabel(r'<< STRING >>')
+ plt.xlabel(r'<< STRING >>')
+ plt.show()
```

#### Block 10 (ipython3) - ⚠️ Modified

> Line count changed: 8 → 1

```diff
- π_arr = np.linspace(0.2, 1, 50)
- models = [create_edm_π(π=π) for π in π_arr]
- gaps = [compute_gap(model) for model in models]
- 
- plt.plot(π_arr, gaps)
- plt.ylabel(r'<< STRING >>')
- plt.xlabel(r'<< STRING >>')
- plt.show()
+ from sympy import Symbol, Lambda, symbols
```


### lectures/french_rev.md

**Summary**: 21 source → 21 target | Score: 🟡 86%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 11 → 16

```diff
  plt.rcParams.update({'<< STRING >>': 12})
  
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
+ 
  base_url = '<< STRING >>'\
  + '<< STRING >>'
```

#### Block 4 (ipython3) - ⚠️ Modified

> Line count changed: 35 → 33

```diff
  
  # << COMMENT >>
- # << COMMENT >>
+ plt.gca().spines['<< STRING >>'].set_visible(False)
+ plt.gca().spines['<< STRING >>'].set_visible(False)
+ plt.gca().set_facecolor('<< STRING >>')
+ plt.gca().set_xlim([1688, 1788])
+ plt.ylabel('<< STRING >>')
  
  plt.tight_layout()
```

#### Block 20 (ipython3) - ⚠️ Modified

> Line count changed: 28 → 26

```diff
  color='<< STRING >>', label='<< STRING >>')
  plt.plot(bal[44:63], a3 + bal[44:63] * b3, color='<< STRING >>')
- 
  plt.xlabel('<< STRING >>')
  plt.ylabel('<< STRING >>')
  plt.legend()
- 
  plt.tight_layout()
  plt.show()
```


### lectures/geom_series.md

**Summary**: 12 source → 12 target | Score: 🟠 67%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 2 block(s) have localization additions

#### Block 1 (ipython) - ⚠️ Modified

> Line count changed: 6 → 12

```diff
+ %matplotlib inline
  import matplotlib.pyplot as plt
  plt.rcParams["<< STRING >>"] = (11, 5) # << COMMENT >>
...
  from sympy import init_printing
  from matplotlib import cm
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 2 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  R = (1 + r)
  return (x_0 * (1 - G**(T + 1) * R**(-T - 1))) / (1 - G * R**(-1))
- # << COMMENT >>
  
+ # << COMMENT >>
  def finite_lease_pv_approx_1(T, g, r, x_0):
  p = x_0 * (T + 1) + x_0 * r * g * (T + 1) / (r - g)
```

#### Block 5 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  rs, gs = (0.9, 0.5, 0.4001, 0.4), (0.4, 0.4, 0.4, 0.5),
- comparisons = (r'<< STRING >>', '<< STRING >>', r'<< STRING >>', '<< STRING >>')
+ comparisons = (r'<< STRING >>', r'<< STRING >>', r'<< STRING >>', r'<< STRING >>')
  for r, g, comp in zip(rs, gs, comparisons):
  ax.plot(finite_lease_pv_true(T, g, r, x_0), label=f'<< STRING >>')
```

#### Block 10 (ipython3) - ⚠️ Modified

> Line count changed: 29 → 31

```diff
  i_0 = 0.3
  g_0 = 0.3
+ 
  # << COMMENT >>
  b = 2/3
...
  ax.set_ylabel('<< STRING >>')
  ax.plot(np.arange(0, T+1), calculate_y(i_0, b, g_0, T, y_init))
+ 
  # << COMMENT >>
  ax.hlines(i_0 / (1 - b) + g_0 / (1 - b), xmin=-1, xmax=101, linestyles='<< STRING >>')
```


### lectures/greek_square.md

**Summary**: 13 source → 13 target | Score: 🟨 92%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 2 → 7

```diff
  import numpy as np
  import matplotlib.pyplot as plt
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```


### lectures/heavy_tails.md

**Summary**: 34 source → 34 target | Score: 🟠 74%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 5 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  :tags: [hide-output]
  
- !pip install --upgrade yfinance wbgapi
+ !pip install --upgrade yfinance pandas_datareader
```

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 10 → 15

```diff
+ import matplotlib as mpl
  import matplotlib.pyplot as plt
  import numpy as np
...
  import statsmodels.api as sm
  
- import wbgapi as wb
+ from pandas_datareader import wb
  from scipy.stats import norm, cauchy
  from pandas.plotting import register_matplotlib_converters
  register_matplotlib_converters()
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 6 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  :tags: [hide-output]
  
- data = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>')
+ data = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>', auto_adjust=False)
```

#### Block 8 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  :tags: [hide-output]
  
- data = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>')
+ data = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>', auto_adjust=False)
```

#### Block 18 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 5

```diff
  data_normal = np.random.normal(size=sample_size)
  sm.qqplot(data_normal, line='<< STRING >>')
+ plt.xlabel("<< STRING >>")
+ plt.ylabel("<< STRING >>")
  plt.show()
```

#### Block 19 (ipython3) - ⚠️ Modified

> Line count changed: 9 → 11

```diff
  labels = ['<< STRING >>', '<< STRING >>', '<< STRING >>']
  for data, label, ax in zip(data_list, labels, axes):
- sm.qqplot(data, line='<< STRING >>', ax=ax, )
+ sm.qqplot(data, line='<< STRING >>', ax=ax)
  ax.set_title(label)
+ ax.set_xlabel('<< STRING >>')
+ ax.set_ylabel('<< STRING >>')
  plt.tight_layout()
  plt.show()
```

#### Block 21 (ipython3) - ⚠️ Modified

> Line count changed: 18 → 21

```diff
  e=2021,
  varnames=None):
+ if c == "<< STRING >>":
+ # << COMMENT >>
+ countries = wb.get_countries()
+ countries_name = countries[countries['<< STRING >>'] != '<< STRING >>']['<< STRING >>'].values
+ c = "<< STRING >>"
  
- df = wb.data.DataFrame(varlist, economy=c, time=range(s, e+1, 1), skipAggs=True)
- df.index.name = '<< STRING >>'
- 
+ df = wb.download(indicator=varlist, country=c, start=s, end=e).stack().unstack(0).reset_index()
+ df = df.drop(['<< STRING >>'], axis=1).transpose()
  if varnames is not None:
- df.columns = variable_names
+ df.columns = varnames
+ df = df[1:]
  
- cntry_mapper = pd.DataFrame(wb.economy.info().items)[['<< STRING >>','<< STRING >>']].set_index('<< STRING >>').to_dict()['<< STRING >>']
- df.index = df.index.map(lambda x: cntry_mapper[x]) # << COMMENT >>
- 
- return df
+ df1 =df[df.index.isin(countries_name)]
+ return df1
```

#### Block 24 (ipython3) - ⚠️ Modified

> Line count changed: 30 → 31, Print statement changes detected

```diff
  df_w = df_w.sort_values('<< STRING >>', ascending=True).copy()
  countries = ['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>']
+ country_names = ['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>']
  N = len(countries)
  
...
  z = z[:top]
  
- empirical_ccdf(z[:top], axs[i], label=c, xlabel='<< STRING >>', add_reg_line=True)
+ empirical_ccdf(z[:top], axs[i], label=country_names[i], xlabel='<< STRING >>', add_reg_line=True)
  
  fig.tight_layout()
```

#### Block 25 (ipython3) - ⚠️ Modified

> Line count changed: 13 → 12

```diff
  
  # << COMMENT >>
- 
  variable_code = ['<< STRING >>', '<< STRING >>']
  variable_names = ['<< STRING >>', '<< STRING >>']
...
  df_gdp1 = extract_wb(varlist=variable_code,
  c="<< STRING >>",
- s=2021,
- e=2021,
+ s="<< STRING >>",
+ e="<< STRING >>",
  varnames=variable_names)
  df_gdp1.dropna(inplace=True)
```


### lectures/inequality.md

**Summary**: 52 source → 52 target | Score: 🟠 77%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 8 block(s) have localization additions

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 6 → 11

```diff
  import wbgapi as wb
  import plotly.express as px
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 4 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  f_vals, l_vals = lorenz_curve(sample)
- ax.plot(f_vals, l_vals, label=f'<< STRING >>', lw=2)
+ ax.plot(f_vals, l_vals, label='<< STRING >>', lw=2)
  ax.plot(f_vals, f_vals, label='<< STRING >>', lw=2)
  
```

#### Block 13 (ipython3) - ⚠️ Modified

> Line count changed: 9 → 7

```diff
- def plot_inequality_measures(x, y, legend, xlabel, ylabel, title=None):
+ def plot_inequality_measures(x, y, legend, xlabel, ylabel):
  fig, ax = plt.subplots()
  ax.plot(x, y, marker='<< STRING >>', label=legend)
  ax.set_xlabel(xlabel)
  ax.set_ylabel(ylabel)
- if title is not None:
- ax.set_title(title)
  ax.legend()
  return fig, ax
```

#### Block 24 (ipython3) - ⚠️ Modified

> Line count changed: 11 → 12

```diff
  figure:
  caption: << CAPTION >>
- name: gini_usa_gbr_nor1
- ---
- ax = data[['<< STRING >>','<< STRING >>', '<< STRING >>']].plot()
- ax.set_xlabel('<< STRING >>')
- ax.set_ylabel('<< STRING >>')
- ax.legend(title="<< STRING >>")
+ \u548C\u632A\u5A01\uFF09"<< STRING >>"美国"<< STRING >>"英国"<< STRING >>"挪威"<< STRING >>"")
  plt.show()
```

#### Block 26 (ipython3) - ⚠️ Modified

> Line count changed: 12 → 13

```diff
  figure:
  caption: << CAPTION >>
- name: gini_usa_gbr_nor2
- ---
- data['<< STRING >>'] = data['<< STRING >>'].ffill()
- ax = data[['<< STRING >>','<< STRING >>', '<< STRING >>']].plot()
- ax.set_xlabel('<< STRING >>')
- ax.set_ylabel('<< STRING >>')
- ax.legend(title="<< STRING >>")
+ \u548C\u632A\u5A01\uFF09"<< STRING >>"美国"<< STRING >>"英国"<< STRING >>"挪威"<< STRING >>"")
  plt.show()
```

#### Block 32 (ipython3) - ⚠️ Modified

> Line count changed: 10 → 12

```diff
  labels = {"<< STRING >>" : "<< STRING >>", "<< STRING >>" : "<< STRING >>"}
  )
+ fig.for_each_trace(lambda t: t.update(name={'<< STRING >>': '<< STRING >>', '<< STRING >>': '<< STRING >>', '<< STRING >>': '<< STRING >>'}[t.name]))
+ 
  fig.update_traces(textposition="<< STRING >>")
  fig.show()
```

#### Block 33 (ipython3) - ⚠️ Modified

> Line count changed: 40 → 39

```diff
  
  # << COMMENT >>
- # << COMMENT >>
  df5 = df4.groupby('<< STRING >>').sum(numeric_only=True).reset_index()
  df8 = df6.groupby('<< STRING >>').sum(numeric_only=True).reset_index()
```

#### Block 35 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  def calculate_top_share(s, p=0.1):
- 
+ # << COMMENT >>
  s = np.sort(s)
  n = len(s)
```

#### Block 37 (ipython3) - ⚠️ Modified

> Line count changed: 12 → 14

```diff
  ---
  mystnb:
+ figure:
+ caption: << CAPTION >>
+ name: top_shares_simulated
  image:
  alt: top_shares_simulated
...
  topshares,
  "<< STRING >>",
- "<< STRING >>",
- "<< STRING >>",
- "<< STRING >>")
+ r"<< STRING >>",
+ r"<< STRING >>")
  plt.show()
```

#### Block 38 (ipython3) - ⚠️ Modified

> Line count changed: 12 → 14

```diff
  ---
  mystnb:
+ figure:
+ caption: << CAPTION >>
+ name: gini_coef_simulated
  image:
  alt: gini_coef_simulated
...
  ginis,
  "<< STRING >>",
- "<< STRING >>",
- "<< STRING >>",
+ r"<< STRING >>",
  "<< STRING >>")
  plt.show()
```

#### Block 39 (ipython3) - ⚠️ Modified

> Line count changed: 12 → 14

```diff
  ---
  mystnb:
+ figure:
+ caption: << CAPTION >>
+ name: lorenz_curve_simulated
  image:
  alt: lorenz_curve_simulated
...
  ax.plot([0,1],[0,1], label=f"<< STRING >>")
  for i in range(len(f_vals)):
- ax.plot(f_vals[i], l_vals[i], label=f"<< STRING >>")
- ax.set_title("<< STRING >>")
+ ax.plot(f_vals[i], l_vals[i], label=fr"<< STRING >>")
  plt.legend()
  plt.show()
```

#### Block 42 (ipython3) - ⚠️ Modified

> Line count changed: 16 → 18

```diff
  ---
  mystnb:
- image:
- alt: top_shares_us_al
- ---
- fig, ax = plt.subplots()
- 
- ax.plot(years, df_topshares["<< STRING >>"], marker='<< STRING >>',\
- label="<< STRING >>")
- ax.plot(years, top_shares_nw, marker='<< STRING >>', label="<< STRING >>")
- 
- ax.set_xlabel("<< STRING >>")
- ax.set_ylabel("<< STRING >>")
- ax.set_title('<< STRING >>')
+ figure:
+ caption: << CAPTION >>
+ \u5179\u66F2\u7EBF"<< STRING >>"topshare_n_wealth"<< STRING >>"净财富-近似值"<< STRING >>"净财富-洛伦兹曲线"<< STRING >>"年份"<< STRING >>"前 $10\%$ 比例")
  ax.legend()
  plt.show()
```


### lectures/inflation_history.md

**Summary**: 19 source → 19 target | Score: 🔴 37%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 11 block(s) have localization additions

#### Block 3 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 9

```diff
  import matplotlib.pyplot as plt
  import matplotlib.dates as mdates
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 5 (ipython3) - ⚠️ Modified

> Line count changed: 23 → 24

```diff
  # << COMMENT >>
  cols = ['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>']
+ cols_cn = ['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>']
  
  fig, ax = plt.subplots(figsize=(10,6))
  
- for col in cols:
+ for col, col_cn in zip(cols, cols_cn):
  ax.plot(df_fig5_befe1914.index,
- df_fig5_befe1914[col], label=col, lw=2)
+ df_fig5_befe1914[col], label=col_cn, lw=2)
  
  ax.legend()
```

#### Block 6 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  fig, ax = plt.subplots(dpi=200)
  
- for col in cols:
+ for col, col_cn in zip(cols, cols_cn):
  ax.plot(df_fig5.index, df_fig5[col], lw=2)
  ax.text(x=df_fig5.index[-1]+2,
- y=df_fig5[col].iloc[-1], s=col)
+ y=df_fig5[col].iloc[-1], s=col_cn)
  
  ax.set_yscale('<< STRING >>')
```

#### Block 10 (ipython3) - ⚠️ Modified

> Line count changed: 17 → 19

```diff
  # << COMMENT >>
  fig, ax = plt.subplots(dpi=200)
- _ = pe_plot(p_seq, e_seq, df_aus.index, lab, ax)
+ ax1 = pe_plot(p_seq, e_seq, df_aus.index, lab, ax)
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  
  plt.show()
```

#### Block 11 (ipython3) - ⚠️ Modified

> Line count changed: 11 → 13

```diff
  # << COMMENT >>
  fig, ax = plt.subplots(dpi=200)
- _ = pr_plot(p_seq, df_aus.index, ax)
+ ax1 = pr_plot(p_seq, df_aus.index, ax)
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  
  plt.show()
```

#### Block 12 (ipython3) - ⚠️ Modified

> Line count changed: 17 → 19

```diff
  # << COMMENT >>
  fig, ax = plt.subplots(dpi=200)
- _ = pe_plot(p_seq, e_seq, df_hun.index, lab, ax)
+ ax1 = pe_plot(p_seq, e_seq, df_hun.index, lab, ax)
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  
  plt.show()
```

#### Block 13 (ipython3) - ⚠️ Modified

> Line count changed: 11 → 12

```diff
  # << COMMENT >>
  fig, ax = plt.subplots(dpi=200)
- _ = pr_plot(p_seq, df_hun.index, ax)
- 
+ ax1 = pr_plot(p_seq, df_hun.index, ax)
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  plt.show()
```

#### Block 15 (ipython3) - ⚠️ Modified

> Line count changed: 8 → 9

```diff
  fig, ax = plt.subplots(dpi=200)
  ax1 = pe_plot(p_seq, e_seq, df_pol.index, lab, ax)
- 
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  plt.show()
```

#### Block 16 (ipython3) - ⚠️ Modified

> Line count changed: 11 → 13

```diff
  # << COMMENT >>
  fig, ax = plt.subplots(dpi=200)
- _ = pr_plot(p_seq, df_pol.index, ax)
+ ax1 = pr_plot(p_seq, df_pol.index, ax)
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  
  plt.show()
```

#### Block 17 (ipython3) - ⚠️ Modified

> Line count changed: 18 → 20

```diff
  fig, ax = plt.subplots(dpi=200)
  ax1 = pe_plot(p_seq, e_seq, df_deu.index, lab, ax)
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  
  plt.show()
```

#### Block 18 (ipython3) - ⚠️ Modified

> Line count changed: 24 → 26

```diff
  fig, ax = plt.subplots(dpi=200)
  ax1 = pe_plot(p_seq, e_seq, df_deu.index, lab, ax)
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  
  plt.show()
```

#### Block 19 (ipython3) - ⚠️ Modified

> Line count changed: 11 → 12

```diff
  # << COMMENT >>
  fig, ax = plt.subplots(dpi=200)
- _ = pr_plot(p_seq, df_deu.index, ax)
- 
+ ax1 = pr_plot(p_seq, df_deu.index, ax)
+ ax1.xaxis.set_major_locator(mdates.MonthLocator(interval=8))
+ ax1.xaxis.set_major_formatter(mdates.DateFormatter('<< STRING >>'))
  plt.show()
```


### lectures/input_output.md

**Summary**: 18 source → 18 target | Score: 🟨 94%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 12 → 16

```diff
  quantecon_book_networks.config("<< STRING >>")
  mpl.rcParams.update(mpl.rcParamsDefault)
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```


### lectures/intro_supply_demand.md

**Summary**: 22 source → 22 target | Score: 🟨 95%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 8

```diff
  import matplotlib.pyplot as plt
  from collections import namedtuple
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```


### lectures/laffer_adaptive.md

**Summary**: 10 source → 10 target | Score: 🟠 60%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 8 → 13

```diff
  import numpy as np
  import matplotlib.pyplot as plt
+ import matplotlib as mpl
  from matplotlib.ticker import MaxNLocator
  from matplotlib.cm import get_cmap
...
  import matplotlib
  from scipy.optimize import root, fsolve
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 4 (ipython3) - ⚠️ Modified

> Line count changed: 38 → 37

```diff
  figure:
  caption: << CAPTION >>
- indicate $\pi_l$ and $\pi_u$.
  name: laffer_curve_adaptive
  width: 500px
...
  plt.plot(x_values, y_values,
  label=f'<< STRING >>')
- for π, label in zip(πs, ['<< STRING >>', '<< STRING >>']):
+ for π, label in zip(πs, [r'<< STRING >>', r'<< STRING >>']):
  plt.text(π, plt.gca().get_ylim()[0]*2,
  label, horizontalalignment='<< STRING >>',
...
  plt.axhline(g, color='<< STRING >>', linewidth=0.5,
  linestyle='<< STRING >>', label='<< STRING >>')
- plt.xlabel('<< STRING >>')
+ plt.xlabel(r'<< STRING >>')
  plt.ylabel('<< STRING >>')
  plt.legend()
```

#### Block 6 (ipython3) - ⚠️ Modified

> Line count changed: 30 → 29

```diff
  # << COMMENT >>
  def p_t(pt):
- return np.log(np.exp(m_seq[t]) + g * np.exp(pt)) \
- - pt + α * ((1-δ)*(pt - p_seq[t-1]) + δ*π_seq[t-1])
+ return np.log(np.exp(m_seq[t]) + g * np.exp(pt)) - pt + α * ((1-δ)*(pt - p_seq[t-1]) + δ*π_seq[t-1])
  
  p_seq[t] = root(fun=p_t, x0=p_seq[t-1]).x[0]
```

#### Block 10 (ipython3) - ⚠️ Modified

> Line count changed: 17 → 15

```diff
  figure:
  caption: << CAPTION >>
- panel, log scale for $m$), $p_t$ (second panel, log scale for $p$), $\pi_t$ (third panel), and $\mu_t$ (bottom
- panel)
  name: pi0_path
  width: 500px
```


### lectures/lake_model.md

**Summary**: 8 source → 8 target | Score: 🟡 88%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 2 → 7

```diff
  import numpy as np
  import matplotlib.pyplot as plt
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```


### lectures/linear_equations.md

**Summary**: 34 source → 34 target | Score: 🟨 97%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 2 → 7

```diff
  import numpy as np
  import matplotlib.pyplot as plt
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```


### lectures/lln_clt.md

**Summary**: 13 source → 13 target | Score: 🟠 69%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 9

```diff
  import matplotlib.pyplot as plt
+ import matplotlib as mpl
  import numpy as np
  import scipy.stats as st
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 6 (ipython3) - ⚠️ Modified

> Line count changed: 21 → 19

```diff
  
  # << COMMENT >>
- 
  sample_means = np.empty(m)
  for j in range(m):
...
  
  # << COMMENT >>
- 
  fig, ax = plt.subplots()
  ax.hist(sample_means, bins=30, alpha=0.5, density=True)
```

#### Block 11 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  '<< STRING >>', lw=2, label=r'<< STRING >>')
  ax.set_xlabel(r"<< STRING >>", size=12)
- ax.set_ylabel(r"<< STRING >>", size=12)
+ ax.set_ylabel("<< STRING >>", size=12)
  
  ax.legend()
```

#### Block 12 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.hist(Y, bins=60, alpha=0.4, density=True)
  ax.set_xlabel(r"<< STRING >>", size=12)
- ax.set_ylabel(r"<< STRING >>", size=12)
+ ax.set_ylabel("<< STRING >>", size=12)
  xgrid = np.linspace(xmin, xmax, 200)
  ax.plot(xgrid, st.norm.pdf(xgrid, scale=σ), '<< STRING >>', lw=2, label=r'<< STRING >>')
```


### lectures/long_run_growth.md

**Summary**: 24 source → 24 target | Score: 🟠 79%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 2 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 5 → 10

```diff
  import numpy as np
  from collections import namedtuple
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 5 (ipython3) - ⚠️ Modified

> Line count changed: 2 → 1

```diff
- code_to_name = data[
- ['<< STRING >>', '<< STRING >>']].drop_duplicates().reset_index(drop=True).set_index(['<< STRING >>'])
+ code_to_name = pd.read_csv("<< STRING >>").set_index('<< STRING >>')
```

#### Block 12 (ipython3) - ⚠️ Modified

> Line count changed: 63 → 62

```diff
  figure:
  caption: << CAPTION >>
+ \u56FD\u3001\u7F8E\u56FD\uFF09"
  name: gdppc_comparison
  tags: [hide-input]
...
  draw_interp_plots(gdp_pc[country].loc[1500:],
  country,
- '<< STRING >>','<< STRING >>',
+ '<< STRING >>', '<< STRING >>',
  color_mapping, code_to_name, 2, False, ax)
  
...
  ax.axvline(event_mid, ymin=1, ymax=event.ymax, color=event.color,
  clip_on=False, alpha=0.15)
- 
- # << COMMENT >>
  draw_events(events, ax)
  plt.show()
```

#### Block 18 (ipython3) - ⚠️ Modified

> Line count changed: 6 → 1

```diff
- # << COMMENT >>
- color_mapping['<< STRING >>'] = color_mapping['<< STRING >>'] # << COMMENT >>
- # << COMMENT >>
- bem = pd.DataFrame(["<< STRING >>"], index=["<< STRING >>"], columns=['<< STRING >>'])
- bem.index.name = '<< STRING >>'
- code_to_name = pd.concat([code_to_name, bem])
+ color_mapping['<< STRING >>'] = color_mapping['<< STRING >>']
```

#### Block 23 (ipython3) - ⚠️ Modified

> Line count changed: 1 → 2

```diff
  regionalgdp_pc.interpolate(method='<< STRING >>', inplace=True)
+ regionalgdp_pc.columns = ['<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>', '<< STRING >>']
```


### lectures/lp_intro.md

**Summary**: 26 source → 26 target | Score: 🟡 88%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 2 block(s) have localization additions

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 5 → 10

```diff
  import matplotlib.pyplot as plt
  from matplotlib.patches import Polygon
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 3 (ipython3) - ⚠️ Modified

> Line count changed: 28 → 29

```diff
  ---
  fig, ax = plt.subplots()
+ 
  # << COMMENT >>
  ax.set_xlim(0,15)
```

#### Block 13 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  if status == pywraplp.Solver.OPTIMAL:
- print('<< STRING >>', solver.Objective().Value())
+ print(f'<< STRING >>')
  x1_sol = round(x1.solution_value(), 3)
  x2_sol = round(x2.solution_value(), 3)
```


### lectures/markov_chains_II.md

**Summary**: 13 source → 13 target | Score: 🟡 85%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 8

```diff
  import quantecon as qe
  import numpy as np
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 11 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  X_bar = (X == 0).cumsum() / np.arange(1, ts_length+1)
  # << COMMENT >>
- ax.plot(X_bar - p, label=f'<< STRING >>')
+ ax.plot(X_bar - p, label=fr'<< STRING >>')
  ax.set_xlabel('<< STRING >>')
  ax.set_ylabel(r'<< STRING >>')
```


### lectures/mle.md

**Summary**: 24 source → 24 target | Score: 🟠 75%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 6 → 11

```diff
  from scipy.integrate import quad
  import matplotlib.pyplot as plt
+ import matplotlib as mpl
  import pandas as pd
  from math import exp
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 8 (ipython3) - ⚠️ Modified

> Code content differs

```diff
- μ_hat = np.mean(ln_sample)
+ μ_hat = np.mean(ln_sample) # << COMMENT >>
  μ_hat
```

#### Block 9 (ipython3) - ⚠️ Modified

> Code content differs

```diff
- num = (ln_sample - μ_hat)**2
- σ_hat = (np.mean(num))**(1/2)
+ num = (ln_sample - μ_hat)**2 # << COMMENT >>
+ σ_hat = (np.mean(num))**(1/2) # << COMMENT >>
  σ_hat
```

#### Block 10 (ipython3) - ⚠️ Modified

> Code content differs

```diff
- dist_lognorm = lognorm(σ_hat, scale = exp(μ_hat))
- x = np.linspace(0,50,10000)
+ dist_lognorm = lognorm(σ_hat, scale = exp(μ_hat)) # << COMMENT >>
+ x = np.linspace(0,50,10000) # << COMMENT >>
  
- fig, ax = plt.subplots()
- ax.set_xlim(-1,20)
+ fig, ax = plt.subplots() # << COMMENT >>
+ ax.set_xlim(-1,20) # << COMMENT >>
  
- ax.hist(sample, density=True, bins=5_000, histtype='<< STRING >>', alpha=0.5)
- ax.plot(x, dist_lognorm.pdf(x), '<< STRING >>', lw=0.5, label='<< STRING >>')
- ax.legend()
- plt.show()
+ ax.hist(sample, density=True, bins=5_000, histtype='<< STRING >>', alpha=0.5) # << COMMENT >>
+ ax.plot(x, dist_lognorm.pdf(x), '<< STRING >>', lw=0.5, label='<< STRING >>') # << COMMENT >>
+ ax.legend() # << COMMENT >>
+ plt.show() # << COMMENT >>
```

#### Block 11 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  def total_revenue(dist):
- integral, _ = quad(lambda x: h(x) * dist.pdf(x), 0, 100_000)
- T = N * integral
- return T
+ integral, _ = quad(lambda x: h(x) * dist.pdf(x), 0, 100_000) # << COMMENT >>
+ T = N * integral # << COMMENT >>
+ return T # << COMMENT >>
```

#### Block 12 (ipython3) - ⚠️ Modified

> Code content differs

```diff
- tr_lognorm = total_revenue(dist_lognorm)
- tr_lognorm
+ tr_lognorm = total_revenue(dist_lognorm) # << COMMENT >>
+ tr_lognorm # << COMMENT >>
```


### lectures/money_inflation.md

**Summary**: 16 source → 16 target | Score: 🟠 75%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 5 → 10

```diff
  import numpy as np
+ import matplotlib as mpl
  import matplotlib.pyplot as plt
  from matplotlib.ticker import MaxNLocator
  plt.rcParams['<< STRING >>'] = 300
  from collections import namedtuple
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 8 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  figure:
  caption: << CAPTION >>
- initial condition $R_0$
  name: R0_path
  width: 500px
  ---
+ 
  # << COMMENT >>
  R0s = np.linspace(msm.γ2/msm.γ1, msm.R_u, 9)
```

#### Block 15 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  # << COMMENT >>
- R = np.insert(P[:-1] / P[1:], 0, np.nan)
+ R = np.insert(P[:-1] / P[1:], 0, np.NAN)
  axes[2].plot(time_steps, R, **line_params)
  
```

#### Block 16 (ipython3) - ⚠️ Modified

> Line count changed: 12 → 10

```diff
  figure:
  caption: << CAPTION >>
- panel, log scale for $m$), $p_t$ (middle panel, log scale for $m$), $R_t$ (bottom
- panel)
  name: p0_path
  width: 500px
```


### lectures/money_inflation_nonlinear.md

**Summary**: 9 source → 9 target | Score: 🟠 67%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 5 → 10

```diff
  from collections import namedtuple
  import numpy as np
+ import matplotlib as mpl
  import matplotlib.pyplot as plt
  from matplotlib.ticker import MaxNLocator
  from scipy.optimize import fsolve
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 6 (ipython3) - ⚠️ Modified

> Line count changed: 14 → 13

```diff
- # << COMMENT >>
  def simulate_seq(p0, model, num_steps):
  λ, g = model.λ, model.g
```

#### Block 9 (ipython3) - ⚠️ Modified

> Line count changed: 20 → 18

```diff
  figure:
  caption: << CAPTION >>
- panel, log scale for $m$), $p_t$ (second panel, log scale for $p$), $\pi_t$ (third panel), and $\mu_t$ (bottom
- panel)
  name: p0_path_nonlin
  width: 500px
```


### lectures/monte_carlo.md

**Summary**: 25 source → 25 target | Score: 🟨 96%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 8

```diff
  import numpy as np
  import matplotlib.pyplot as plt
+ import matplotlib as mpl
  from numpy.random import randn
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```


### lectures/networks.md

**Summary**: 42 source → 42 target | Score: 🟨 90%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  :tags: [hide-output]
  
- !pip install quantecon-book-networks pandas-datareader
+ !pip install quantecon quantecon-book-networks wbgapi
```

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 11 → 16

```diff
  
  import matplotlib.patches as mpatches
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 24 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 3

```diff
  iG7 = [G7.in_degree(v) for v in G7.nodes()] # << COMMENT >>
- 
  for i, d in enumerate(iG7):
  print(i+1, d)
```

#### Block 35 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  # << COMMENT >>
  
+ G = nx.DiGraph()
+ 
+ G.add_nodes_from(np.arange(8)) # << COMMENT >>
+ 
+ G.add_edges_from([(0,1),(0,3), # << COMMENT >>
+ (1,0),
+ (2,4),
+ (3,2),(3,4),(3,7),
+ (4,3),
+ (5,4),(5,6),
+ (6,3),(6,5),
+ (7,0)])
+ 
+ nx.draw_networkx(G, pos=nx.circular_layout(G), node_color='<< STRING >>', node_size=500, with_labels=True)
+ 
  plt.show()
```


### lectures/olg.md

**Summary**: 22 source → 22 target | Score: 🟨 95%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 9

```diff
  from collections import namedtuple
  import matplotlib.pyplot as plt
+ 
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```


### lectures/prob_dist.md

**Summary**: 57 source → 57 target | Score: 🟠 77%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 3 block(s) have localization additions

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 6 → 11

```diff
  import matplotlib.pyplot as plt
+ import matplotlib as mpl
  import pandas as pd
  import numpy as np
...
  import scipy.stats
  import seaborn as sns
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 26 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.pdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_xlabel('<< STRING >>')
  ax.set_ylabel('<< STRING >>')
```

#### Block 27 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.cdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_ylim(0, 1)
  ax.set_xlabel('<< STRING >>')
```

#### Block 31 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.cdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_ylim(0, 1)
  ax.set_xlim(0, 3)
```

#### Block 34 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.pdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_xlabel('<< STRING >>')
  ax.set_ylabel('<< STRING >>')
```

#### Block 35 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.cdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_ylim(0, 1)
  ax.set_xlabel('<< STRING >>')
```

#### Block 38 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.pdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_xlabel('<< STRING >>')
  ax.set_ylabel('<< STRING >>')
```

#### Block 39 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.cdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_ylim(0, 1)
  ax.set_xlabel('<< STRING >>')
```

#### Block 42 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.pdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_xlabel('<< STRING >>')
  ax.set_ylabel('<< STRING >>')
```

#### Block 43 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  ax.plot(x_grid, u.cdf(x_grid),
  alpha=0.5, lw=2,
- label=rf'<< STRING >>')
+ label=fr'<< STRING >>')
  ax.set_ylim(0, 1)
  ax.set_xlabel('<< STRING >>')
```

#### Block 47 (ipython3) - ⚠️ Modified

> Line count changed: 6 → 7

```diff
  :tags: [hide-output]
  
- df = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>', interval='<< STRING >>')
+ df = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>',
+ interval='<< STRING >>', auto_adjust=False)
  prices = df['<< STRING >>']
  x_amazon = prices.pct_change()[1:] * 100
```

#### Block 53 (ipython3) - ⚠️ Modified

> Line count changed: 5 → 6

```diff
  :tags: [hide-output]
  
- df = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>', interval='<< STRING >>')
+ df = yf.download('<< STRING >>', '<< STRING >>', '<< STRING >>',
+ interval='<< STRING >>', auto_adjust=False)
  prices = df['<< STRING >>']
  x_costco = prices.pct_change()[1:] * 100
```

#### Block 57 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  μ, σ = 0, 1
  u = scipy.stats.norm(μ, σ)
- N = 2000 # << COMMENT >>
+ N = 2000
  x_draws = u.rvs(N)
  x_grid = np.linspace(-4, 4, 200)
```


### lectures/pv.md

**Summary**: 8 source → 8 target | Score: 🟠 75%
  
**📍 i18n**: Localization patterns detected: CJK font family, Chinese/CJK font configuration. 2 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 2 → 6

```diff
  import numpy as np
  import matplotlib.pyplot as plt
+ from matplotlib import font_manager
+ fontP = font_manager.FontProperties()
+ fontP.set_family('<< STRING >>')
+ fontP.set_size(14)
```

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 12 → 17

```diff
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
+ 
  T = 6
  current_d = 1.0
```


### lectures/schelling.md

**Summary**: 8 source → 8 target | Score: 🟠 63%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 8

```diff
  from math import sqrt
  import numpy as np
+ import matplotlib as mpl
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 2 (ipython3) - ⚠️ Modified

> Line count changed: 47 → 45

```diff
  
  # << COMMENT >>
- # << COMMENT >>
  for agent in agents:
  if self != agent:
```

#### Block 6 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  if flip_prob > 0:
  # << COMMENT >>
+ U = uniform()
+ if U < flip_prob:
+ current_type = types[i]
+ types[i] = 0 if current_type == 1 else 1
+ 
+ # << COMMENT >>
+ if current_iter % test_freq == 0:
+ cycle = current_iter / n
+ plot_distribution(locations, types, f'<< STRING >>')
  if count_happy(locations, types) == n:
  print(f"<< STRING >>")
```


### lectures/short_path.md

**Summary**: 9 source → 9 target | Score: 🟡 89%

#### Block 5 (python3) - ⚠️ Modified

> Code content differs

```diff
  
  # << COMMENT >>
+ Q = np.full((num_nodes, num_nodes), np.inf)
+ 
+ # << COMMENT >>
+ with open(in_file) as infile:
+ for line in infile:
+ elements = line.split('<< STRING >>')
  node = elements.pop(0)
  node = int(node[4:]) # << COMMENT >>
```


### lectures/simple_linear_regression.md

**Summary**: 41 source → 41 target | Score: 🟠 76%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 8

```diff
  import numpy as np
  import pandas as pd
+ import matplotlib as mpl
  import matplotlib.pyplot as plt
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 2 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  x = [32, 21, 24, 35, 10, 11, 22, 21, 27, 2]
- y = [2000,1000,1500,2500,500,900,1100,1500,1800, 250]
+ y = [2000, 1000, 1500, 2500, 500, 900, 1100, 1500, 1800, 250]
  df = pd.DataFrame([x,y]).T
  df.columns = ['<< STRING >>', '<< STRING >>']
```

#### Block 3 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  y='<< STRING >>',
  kind='<< STRING >>',
- ylabel='<< STRING >>',
+ ylabel=r'<< STRING >>',
  xlabel='<< STRING >>'
  )
```

#### Block 5 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  fig, ax = plt.subplots()
  ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax)
- ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax)
+ ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, label=r'<< STRING >>')
  plt.show()
```

#### Block 7 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  fig, ax = plt.subplots()
  ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax)
- ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax)
+ ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, label=r'<< STRING >>')
  plt.show()
```

#### Block 9 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  fig, ax = plt.subplots()
  ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax)
- ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, color='<< STRING >>')
+ yax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, color='<< STRING >>', label=r'<< STRING >>')
  plt.show()
```

#### Block 12 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  fig, ax = plt.subplots()
  ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax)
- ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, color='<< STRING >>')
+ yax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, color='<< STRING >>', label=r'<< STRING >>')
  plt.vlines(df['<< STRING >>'], df['<< STRING >>'], df['<< STRING >>'], color='<< STRING >>')
  plt.show()
```

#### Block 21 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  fig, ax = plt.subplots()
  ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax)
- ax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, color='<< STRING >>')
+ yax = df.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, color='<< STRING >>', label=r'<< STRING >>')
  plt.vlines(df['<< STRING >>'], df['<< STRING >>'], df['<< STRING >>'], color='<< STRING >>');
```

#### Block 30 (ipython3) - ⚠️ Modified

> Code content differs

```diff
- le_years.loc['<< STRING >>'].plot()
+ le_years.loc['<< STRING >>'].plot(xlabel="<< STRING >>")
```

#### Block 41 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  fig, ax = plt.subplots()
- data.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax)
- data.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, color='<< STRING >>')
+ data.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, xlabel="<< STRING >>", ylabel="<< STRING >>")
+ data.plot(x='<< STRING >>',y='<< STRING >>', kind='<< STRING >>', ax=ax, color='<< STRING >>', xlabel="<< STRING >>", label="<< STRING >>")
  plt.vlines(data['<< STRING >>'], data['<< STRING >>'], data['<< STRING >>'], color='<< STRING >>')
```


### lectures/supply_demand_heterogeneity.md

**Summary**: 9 source → 9 target | Score: 🔴 44%

#### Block 3 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  bs = [np.array([5, 5]), # << COMMENT >>
+ np.array([5, 5])] # << COMMENT >>
  
  es = [np.array([0, 2]), # << COMMENT >>
+ np.array([2, 0])] # << COMMENT >>
  
  EE = ExchangeEconomy(Π, bs, es)
```

#### Block 4 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  EE.bs = [np.array([6, 5]), # << COMMENT >>
+ np.array([5, 6])] # << COMMENT >>
  
  p, c_s, μ_s = EE.competitive_equilibrium()
```

#### Block 5 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  EE.es = [np.array([0.5, 0.5]), # << COMMENT >>
+ np.array([1, 1])] # << COMMENT >>
  
  p, c_s, μ_s = EE.competitive_equilibrium()
```

#### Block 6 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  EE.bs = [np.array([4, 6]), # << COMMENT >>
+ np.array([6, 4])] # << COMMENT >>
  
  EE.es = [np.array([0, 2]), # << COMMENT >>
+ np.array([2, 0])] # << COMMENT >>
  
  p, c_s, μ_s = EE.competitive_equilibrium()
```

#### Block 7 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  bs = [np.array([5, 5]), # << COMMENT >>
+ np.array([5, 5])] # << COMMENT >>
  
  es = [np.array([1, 1]), # << COMMENT >>
+ np.array([1, 1])] # << COMMENT >>
  
  Ws = [0.5, -0.5]
```


### lectures/supply_demand_multiple_goods.md

**Summary**: 22 source → 22 target | Score: 🟠 68%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 4 → 9

```diff
  # << COMMENT >>
  import numpy as np
+ import matplotlib as mpl
  import matplotlib.pyplot as plt
  from scipy.linalg import inv
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 2 (ipython3) - ⚠️ Modified

> Code content differs

```diff
  
  if any(c < 0):
- print('<< STRING >>', c)
+ print(f'<< STRING >>')
  raise Exception('<< STRING >>')
  
```

#### Block 10 (ipython3) - ⚠️ Modified

> Line count changed: 14 → 13

```diff
  print('<< STRING >>', c.item())
  
- # << COMMENT >>
  plot_competitive_equilibrium(PE)
```

#### Block 12 (ipython3) - ⚠️ Modified

> Line count changed: 8 → 7

```diff
  print('<< STRING >>', c.item())
  
- # << COMMENT >>
  plot_competitive_equilibrium(PE)
```

#### Block 14 (ipython3) - ⚠️ Modified

> Line count changed: 9 → 8

```diff
  print('<< STRING >>', c.item())
  
- # << COMMENT >>
  plot_competitive_equilibrium(PE)
```

#### Block 20 (ipython3) - ⚠️ Modified

> Line count changed: 55 → 54

```diff
  
  # << COMMENT >>
- marg_cost = lambda x: h + H * x
- marg_rev = lambda x: -2 * 1 / μ * Π * Π * x + 1 / μ * Π * b
- demand_inv = lambda x: 1 / μ * (Π * b - Π * Π * x)
+ marg_cost = lambda x: h + H * x # << COMMENT >>
+ marg_rev = lambda x: -2 * 1 / μ * Π * Π * x + 1 / μ * Π * b # << COMMENT >>
+ demand_inv = lambda x: 1 / μ * (Π * b - Π * Π * x) # << COMMENT >>
  
  xs = np.linspace(0, 2 * c, 100)
```

#### Block 22 (ipython3) - ⚠️ Modified

> Line count changed: 18 → 17

```diff
  print('<< STRING >>', q.item())
  
- # << COMMENT >>
  plot_monopoly(M)
```


### lectures/unpleasant.md

**Summary**: 10 source → 10 target | Score: 🟡 80%
  
**📍 i18n**: Localization patterns detected: Chinese/CJK font configuration. 1 block(s) have localization additions

#### Block 1 (ipython3) - ⚠️ Modified

> Line count changed: 3 → 8

```diff
  import matplotlib.pyplot as plt
  from collections import namedtuple
+ import matplotlib as mpl
+ 
+ FONTPATH = "<< STRING >>"
+ mpl.font_manager.fontManager.addfont(FONTPATH)
+ plt.rcParams['<< STRING >>'] = ['<< STRING >>']
```

#### Block 8 (ipython3) - ⚠️ Modified

> Line count changed: 44 → 43

```diff
  def simulate(m0, model, length=15, p0_guess=1):
- 
  # << COMMENT >>
  γ1, γ2, g = model.γ1, model.γ2, model.g
```


---
*Generated by tool-alignment*